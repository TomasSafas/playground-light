module.exports = function () {
    let config = {
        scss: {
            src: [
                "scss/**/*.scss"
            ],
            dest: "content/"
        }
    };

    return config;
};